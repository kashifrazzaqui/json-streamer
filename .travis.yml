language : python
sudo: required
python:
    - "3.3"
    - "3.4"
before_install:
    - git clone https://github.com/lloyd/yajl.git
    - cd yajl
    - sudo ./configure
    - sudo make install
    - cd ..
install:
    - pip install again
    - pip install pytest
script: py.test
